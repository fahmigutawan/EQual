<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.equal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EQual">
        <activity
            android:name=".activity_splashscreen.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity_login.LoginActivity"
            android:windowSoftInputMode="stateVisible|adjustPan"
            />
        <activity android:name=".activity_register.RegisterActivity"
            android:windowSoftInputMode="stateVisible|adjustPan"/>
        <activity android:name=".activity_onboard.OnboardingActivity"/>
        <activity android:name=".activity_home.HomeActivity"/>
    </application>
</manifest>